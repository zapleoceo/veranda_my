name: Deploy to veranda.my

on:
  push:
    branches: ["main"]

concurrency:
  group: deploy-veranda-my
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Ensure rsync
        shell: bash
        run: |
          if ! command -v rsync >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y rsync
          fi

      - name: Configure SSH
        shell: bash
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          if [ -n "${{ secrets.SSH_KNOWN_HOSTS }}" ]; then
            echo "${{ secrets.SSH_KNOWN_HOSTS }}" >> ~/.ssh/known_hosts
          else
            ssh-keyscan -p "${{ secrets.SSH_PORT }}" -H "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts
          fi

      - name: Check SSH connectivity
        shell: bash
        run: |
          set -euo pipefail
          echo "Checking TCP connectivity to ${{ secrets.SSH_HOST }}:${{ secrets.SSH_PORT }} ..."
          timeout 5 bash -lc "cat < /dev/null > /dev/tcp/${{ secrets.SSH_HOST }}/${{ secrets.SSH_PORT }}"
          echo "TCP OK"

      - name: Deploy files
        shell: bash
        run: |
          set -euo pipefail
          SSH_OPTS="-p ${{ secrets.SSH_PORT }} -i ~/.ssh/id_ed25519 -o BatchMode=yes -o StrictHostKeyChecking=yes -o ConnectTimeout=10 -o ConnectionAttempts=3 -o ServerAliveInterval=15 -o ServerAliveCountMax=3"

          for i in 1 2 3; do
            echo "rsync attempt ${i}/3"
            rsync -az \
              --delete \
              --delete-after \
              --exclude ".env" \
              --exclude ".env.*" \
              --exclude "*.log" \
              --exclude "telegram.log" \
              --exclude "prob_close_backfill.log" \
              --exclude "tmp/" \
              --exclude "cache/" \
              --exclude ".cache/" \
              --exclude "vendor/" \
              --exclude "logs/" \
              --exclude "payday2/local_config.json" \
              --exclude "wa_listener/auth/" \
              --exclude "wa_listener/qr_state.json" \
              --exclude "wa_listener/node_modules/" \
              --exclude "wa_listener/package-lock.json" \
              --exclude ".git/" \
              --exclude ".github/" \
              -e "ssh ${SSH_OPTS}" \
              ./ ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.DEPLOY_PATH }}/ && break
            sleep $((i * 3))
          done

      - name: Link static assets into public/
        shell: bash
        run: |
          set -euo pipefail
          SSH_OPTS="-p ${{ secrets.SSH_PORT }} -i ~/.ssh/id_ed25519 -o BatchMode=yes -o StrictHostKeyChecking=yes -o ConnectTimeout=10 -o ConnectionAttempts=3 -o ServerAliveInterval=15 -o ServerAliveCountMax=3"
          ssh ${SSH_OPTS} "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" "
            D='${{ secrets.DEPLOY_PATH }}'
            ln -sfn \$D/assets \$D/public/assets
            rm -rf \$D/public/tr3
          "

      - name: Install Composer dependencies (server)
        shell: bash
        run: |
          set -euo pipefail
          SSH_OPTS="-p ${{ secrets.SSH_PORT }} -i ~/.ssh/id_ed25519 -o BatchMode=yes -o StrictHostKeyChecking=yes -o ConnectTimeout=10 -o ConnectionAttempts=3 -o ServerAliveInterval=15 -o ServerAliveCountMax=3"
          for i in 1 2 3; do
            echo "composer attempt ${i}/3"
            ssh ${SSH_OPTS} "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" \
              "cd '${{ secrets.DEPLOY_PATH }}' && /opt/php82/bin/php /usr/local/bin/composer install --no-dev --optimize-autoloader --no-interaction 2>&1" && break
            sleep $((i * 3))
          done

      - name: Remove debug files (server)
        shell: bash
        run: |
          set -euo pipefail
          SSH_OPTS="-p ${{ secrets.SSH_PORT }} -i ~/.ssh/id_ed25519 -o BatchMode=yes -o StrictHostKeyChecking=yes -o ConnectTimeout=10 -o ConnectionAttempts=3 -o ServerAliveInterval=15 -o ServerAliveCountMax=3"
          for i in 1 2 3; do
            echo "ssh attempt ${i}/3"
            ssh ${SSH_OPTS} "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" \
              "rm -f '${{ secrets.DEPLOY_PATH }}/mail.php' '${{ secrets.DEPLOY_PATH }}/access.php'" && break
            sleep $((i * 3))
          done

      - name: Verify PHP syntax (server)
        shell: bash
        run: |
          set -euo pipefail
          SSH_OPTS="-p ${{ secrets.SSH_PORT }} -i ~/.ssh/id_ed25519 -o BatchMode=yes -o StrictHostKeyChecking=yes -o ConnectTimeout=10 -o ConnectionAttempts=3 -o ServerAliveInterval=15 -o ServerAliveCountMax=3"
          for i in 1 2 3; do
            echo "ssh attempt ${i}/3"
            ssh ${SSH_OPTS} "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" \
              "cd '${{ secrets.DEPLOY_PATH }}' && PHP=/opt/php82/bin/php && \$PHP -l public/index.php && \$PHP -l cron/kitchen_sync.php && \$PHP -l cron/telegram_alerts.php" && break
            sleep $((i * 3))
          done
